package interest_activities

templ introSection(phrases map[string]map[string]string) {
	<div class="clubs-intro animate-on-scroll">
		<div class="container">
			<div class="row justify-content-center">
				<div class="col-lg-10 text-center">
					<h3 class="mb-4">{ phrases["intro"]["title"] }</h3>
					<p class="lead">{ phrases["intro"]["description"] }</p>
				</div>
			</div>
		</div>
	</div>
}
