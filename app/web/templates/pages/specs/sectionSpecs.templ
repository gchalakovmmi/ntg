package specs

templ sectionSpecs(phrases map[string]map[string]string) {
	<section class="section">
		<div class="container">
			<div class="section-title text-center mb-5 animate-on-scroll">
				<h2>{ phrases["rpp"]["title"] }</h2>
				<p class="text-muted">{ phrases["rpp"]["subtitle"] }</p>
			</div>
			<div class="documents-container">
				<!-- Document Row 1 -->
				<div class="document-row animate-on-scroll">
					<div class="document-info">
						<div class="document-icon">
							<i class="fas fa-file-pdf text-danger"></i>
						</div>
						<div class="document-details">
							<h5 class="document-title">{ phrases["documents"]["title"] }</h5>
							<p class="document-description text-muted">{ phrases["documents"]["title_subtitle"] }</p>
						</div>
					</div>
					<div class="document-action">
						<a href="/docs/documents/specs/1690175 NTG Dni na profesiite 21-22.04.2023.pptx" class="btn btn-success btn-sm">
							<i class="fas fa-download me-1"></i>
							{ phrases["documents"]["view_download"] }
						</a>
					</div>
				</div>
			</div>
		</div>
	</section>
}
